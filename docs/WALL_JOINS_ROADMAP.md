# ROADMAP: Интеграция системы wall joins (стыковка стен)

## 📌 Цель проекта
Интегрировать существующую систему `WallAttachmentSystem` в рендеринг стен для автоматической корректной стыковки углов в зависимости от режима привязки (Core, FinishExterior, FinishInterior).

---

## 🎯 ПРОБЛЕМА

### Текущая ситуация:
- ✅ **WallAttachmentSystem РЕАЛИЗОВАН** (все методы существуют)
- ❌ **НЕ интегрирован в WallRenderer**
- ❌ Стены рендерятся как простые прямоугольники БЕЗ стыковки углов
- ❌ В местах пересечения стен видны зазоры или наложения

### Желаемое поведение (как в Revit):

#### 1. Осевая привязка (Core):
- Стены соединяются по центральным осям
- Формируется miter join (скошенный угол)
- Обе грани (внешняя и внутренняя) пересекаются правильно

#### 2. Внешняя привязка (FinishExterior):
- Стены соединяются по внешним граням
- Внешний угол идеально ровный (без зазора)
- Внутренний угол "дорисовывается" до пересечения

#### 3. Внутренняя привязка (FinishInterior):
- Стены соединяются по внутренним граням
- Внутренний угол идеально ровный
- Внешний угол "дорисовывается" до пересечения

---

## 📋 ЭТАПЫ РЕАЛИЗАЦИИ

---

### ✅ ЭТАП 0: Анализ и подготовка (ЗАВЕРШЕН)
**Статус:** ✅ Завершен

#### Подзадачи:
- [x] Проанализированы скриншоты Revit (1-6)
- [x] Изучен WallAttachmentSystem.h
- [x] Найдена проблема: система не интегрирована в рендеринг
- [x] Подтверждено: все методы уже реализованы

#### Что есть в WallAttachmentSystem:
```cpp
// Три метода построения геометрии углов:
BuildCoreMiterJoin()      // для Core mode
BuildExteriorJoin()       // для FinishExterior mode
BuildInteriorJoin()       // для FinishInterior mode

// Обнаружение стыков:
FindJoins()               // находит L, T, X, Collinear соединения

// Вычисление геометрии:
CalculateJoinGeometry()   // выбирает правильный метод
```

#### Критерии завершения:
- ✅ Понята архитектура системы
- ✅ Определен план интеграции

---

### ⬜ ЭТАП 1: Интеграция WallAttachmentSystem в WallRenderer
**Статус:** 🕒 Ожидает

#### Подзадачи:
- [ ] 1.1. Добавить `#include "WallAttachmentSystem.h"` в WallRenderer.h
- [ ] 1.2. Добавить member: `WallAttachmentSystem m_attachmentSystem;`
- [ ] 1.3. Создать метод `FindAllWallJoins(document)` → возвращает `std::vector<AttachmentJoinInfo>`
- [ ] 1.4. Создать метод `BuildJoinGeometry(join)` → вызывает соответствующий BuildXXXJoin
- [ ] 1.5. Создать метод `RenderWallJoin(session, camera, join)` → рисует геометрию стыка

#### Изменения в коде:

**Файл:** `WallRenderer.h` (добавить в private members ~строка 490)

```cpp
// Wall attachment system for join geometry
WallAttachmentSystem m_attachmentSystem;

// Cache of join geometry
std::unordered_map<uint64_t, std::vector<AttachmentJoinInfo>> m_joinCache;
```

**Метод 1:** FindAllWallJoins
```cpp
std::vector<AttachmentJoinInfo> FindAllWallJoins(const DocumentModel& document)
{
    const auto& walls = document.GetWalls();

    // Использовать WallAttachmentSystem::FindJoins
    std::vector<AttachmentJoinInfo> joins;

    for (size_t i = 0; i < walls.size(); ++i)
    {
        for (size_t j = i + 1; j < walls.size(); ++j)
        {
            auto join = m_attachmentSystem.FindJoin(*walls[i], *walls[j], 5.0);
            if (join.IsValid())
            {
                joins.push_back(join);
            }
        }
    }

    return joins;
}
```

#### Критерии завершения:
- ⬜ WallAttachmentSystem интегрирован в WallRenderer
- ⬜ Метод FindAllWallJoins находит стыки
- ⬜ Компиляция проходит без ошибок

---

### ⬜ ЭТАП 2: Модификация геометрии стен с учетом стыков
**Статус:** 🕒 Ожидает

#### Подзадачи:
- [ ] 2.1. Модифицировать `WallPlanGeometryBuilder::BuildBasic()` для учета стыков
- [ ] 2.2. Добавить параметр `joins` в метод BuildBasic
- [ ] 2.3. Обрезать/расширить boundary path стены в местах стыков
- [ ] 2.4. Хранить информацию о стыках в `WallPlanGeometry`

#### Текущая проблема:
`WallPlanGeometryBuilder::BuildBasic()` создает простой прямоугольник:
```cpp
// 4 Corner points of the wall "static contour"
WorldPoint p1(startAxis.X + perp.X * halfThickness, ...);
WorldPoint p2(startAxis.X - perp.X * halfThickness, ...);
WorldPoint p3(endAxis.X - perp.X * halfThickness, ...);
WorldPoint p4(endAxis.X + perp.X * halfThickness, ...);

result.BoundaryPath.AddPoint(p1);
result.BoundaryPath.AddPoint(p4);
result.BoundaryPath.AddPoint(p3);
result.BoundaryPath.AddPoint(p2);
```

#### Новая логика:
```cpp
// Если стена имеет стыки на концах, заменить p1-p2 или p3-p4
// на точки из CornerPolygon стыка

if (hasJoinAtStart)
{
    // Заменить p1, p2 на точки из join.CornerPolygon
}

if (hasJoinAtEnd)
{
    // Заменить p3, p4 на точки из join.CornerPolygon
}
```

#### Критерии завершения:
- ⬜ Boundary path стены корректно обрезается/расширяется в местах стыков
- ⬜ Нет зазоров между стенами
- ⬜ Нет наложений

---

### ⬜ ЭТАП 3: Рендеринг геометрии стыков
**Статус:** 🕒 Ожидает

#### Подзадачи:
- [ ] 3.1. Модифицировать `DrawWall()` для передачи информации о стыках
- [ ] 3.2. Модифицировать `DrawWallWithGeometricOutline()` для учета стыков
- [ ] 3.3. Убедиться, что обводка правильно рендерится в углах
- [ ] 3.4. Добавить рендеринг CornerPolygon в местах стыков

#### Изменения:

**В методе Draw():**
```cpp
void Draw(...)
{
    // 1. Найти все стыки
    auto joins = FindAllWallJoins(document);

    // 2. Для каждой стены найти её стыки
    for (const auto& wall : walls)
    {
        auto wallJoins = GetJoinsForWall(wall->GetId(), joins);
        DrawWall(session, camera, *wall, viewSettings, wallJoins, ...);
    }
}
```

**В методе DrawWall():**
```cpp
void DrawWall(..., const std::vector<AttachmentJoinInfo>& joins, ...)
{
    // Build geometry with joins
    WallPlanGeometry geometry = GetOrBuildGeometry(wall, detailLevel, joins);

    // Render outline (уже с учетом стыков)
    DrawWallWithGeometricOutline(...);
}
```

#### Критерии завершения:
- ⬜ Стыки корректно рендерятся
- ⬜ Обводка не имеет артефактов в углах
- ⬜ Геометрия соответствует скриншотам Revit

---

### ⬜ ЭТАП 4: Тестирование для режима Core
**Статус:** 🕒 Ожидает

#### Подзадачи:
- [ ] 4.1. Создать тестовую сцену: 2 стены под 90° (L-join)
- [ ] 4.2. Установить обе стены в режим Core
- [ ] 4.3. Проверить: угол формируется правильно (скриншоты 1-2)
- [ ] 4.4. Проверить разные углы: 45°, 60°, 90°, 135°
- [ ] 4.5. Проверить T-join (3 стены)

#### Тесты:
1. **L-Join 90°** - Классический угол
2. **L-Join 45°** - Острый угол
3. **L-Join 135°** - Тупой угол
4. **T-Join** - Одна стена упирается в другую
5. **X-Join** - 4 стены пересекаются

#### Критерии завершения:
- ⬜ Все тесты проходят для режима Core
- ⬜ Геометрия идентична Revit (скриншоты 1-2)
- ⬜ Нет визуальных артефактов

---

### ⬜ ЭТАП 5: Тестирование для режима FinishExterior
**Статус:** 🕒 Ожидает

#### Подзадачи:
- [ ] 5.1. Создать тестовую сцену: 2 стены под 90° (L-join)
- [ ] 5.2. Установить обе стены в режим FinishExterior
- [ ] 5.3. Проверить: внешний угол идеально ровный (скриншоты 3-4)
- [ ] 5.4. Проверить: внутренний угол "дорисован"
- [ ] 5.5. Проверить разные углы

#### Особенности FinishExterior:
- Внешний угол должен быть БЕЗ зазора
- Внутренний угол может иметь дополнительную геометрию
- Линия привязки = внешняя грань

#### Критерии завершения:
- ⬜ Все тесты проходят для режима FinishExterior
- ⬜ Геометрия идентична Revit (скриншоты 3-4)
- ⬜ Внешние углы идеально ровные

---

### ⬜ ЭТАП 6: Тестирование для режима FinishInterior
**Статус:** 🕒 Ожидает

#### Подзадачи:
- [ ] 6.1. Создать тестовую сцену: 2 стены под 90° (L-join)
- [ ] 6.2. Установить обе стены в режим FinishInterior
- [ ] 6.3. Проверить: внутренний угол идеально ровный (скриншоты 5-6)
- [ ] 6.4. Проверить: внешний угол "дорисован"
- [ ] 6.5. Проверить разные углы

#### Особенности FinishInterior:
- Внутренний угол должен быть БЕЗ зазора
- Внешний угол может иметь дополнительную геометрию
- Линия привязки = внутренняя грань

#### Критерии завершения:
- ⬜ Все тесты проходят для режима FinishInterior
- ⬜ Геометрия идентична Revit (скриншоты 5-6)
- ⬜ Внутренние углы идеально ровные

---

### ⬜ ЭТАП 7: Смешанные режимы
**Статус:** 🕒 Ожидает

#### Подзадачи:
- [ ] 7.1. Протестировать: Core + FinishExterior
- [ ] 7.2. Протестировать: Core + FinishInterior
- [ ] 7.3. Протестировать: FinishExterior + FinishInterior
- [ ] 7.4. Убедиться, что BuildMixedJoin() работает корректно

#### Критерии завершения:
- ⬜ Смешанные режимы работают без ошибок
- ⬜ Геометрия логична и не имеет артефактов

---

### ⬜ ЭТАП 8: Оптимизация и кэширование
**Статус:** 🕒 Ожидает

#### Подзадачи:
- [ ] 8.1. Добавить кэширование стыков (m_joinCache)
- [ ] 8.2. Инвалидировать кэш при изменении стен
- [ ] 8.3. Профилирование: проверить производительность при 50+ стенах
- [ ] 8.4. Оптимизировать FindAllWallJoins (сейчас O(n²))

#### Оптимизации:
```cpp
// Spatial hashing для быстрого поиска стыков
// Вместо O(n²) → O(n)

// Кэширование:
std::unordered_map<uint64_t, std::vector<AttachmentJoinInfo>> m_joinCache;

// Инвалидация:
void InvalidateJoinCache(uint64_t wallId)
{
    m_joinCache.erase(wallId);
    // Также инвалидировать все стены, соединенные с wallId
}
```

#### Критерии завершения:
- ⬜ FPS не упал при наличии стыков
- ⬜ Кэширование работает корректно
- ⬜ 100+ стен рендерятся без лагов

---

### ⬜ ЭТАП 9: Документация и финализация
**Статус:** 🕒 Ожидает

#### Подзадачи:
- [ ] 9.1. Обновить CLAUDE.md с описанием системы wall joins
- [ ] 9.2. Добавить комментарии в код
- [ ] 9.3. Создать примеры использования
- [ ] 9.4. Обновить юнит-тесты

#### Документация:
```markdown
## Wall Joins System

Автоматическая стыковка стен в зависимости от режима привязки.

### Режимы привязки:
- **Core**: стены соединяются по центральным осям
- **FinishExterior**: стены соединяются по внешним граням
- **FinishInterior**: стены соединяются по внутренним граням

### Использование:
Система работает автоматически при рендеринге. Стыки обнаруживаются
и геометрия углов корректируется в зависимости от attachment mode обеих стен.
```

#### Критерии завершения:
- ⬜ Вся документация обновлена
- ⬜ Комментарии в коде актуальны
- ⬜ Примеры добавлены

---

### ⬜ ЭТАП 10: Code review и тестирование
**Статус:** 🕒 Ожидает

#### Подзадачи:
- [ ] 10.1. Самопроверка кода
- [ ] 10.2. Финальное тестирование всех режимов
- [ ] 10.3. Проверка edge cases
- [ ] 10.4. Git commit с описанием изменений

#### Edge cases для тестирования:
- Очень острые углы (< 15°)
- Очень тупые углы (> 165°)
- Стены разной толщины
- Короткие стены (< 200 мм)
- 4+ стен в одной точке

#### Критерии завершения:
- ⬜ Все тесты пройдены
- ⬜ Edge cases обработаны
- ⬜ Код закоммичен в git

---

## 📊 ПРОГРЕСС

### Общий прогресс: 0% (подготовка завершена)
- ✅ Этап 0: 100% (анализ завершен)
- ⬜ Этап 1: 0% (интеграция WallAttachmentSystem)
- ⬜ Этап 2: 0% (модификация геометрии)
- ⬜ Этап 3: 0% (рендеринг)
- ⬜ Этапы 4-6: 0% (тестирование режимов)
- ⬜ Этапы 7-10: 0% (доработка)

---

## 🚨 КРИТИЧЕСКИЕ РИСКИ

### 1. Производительность при большом количестве стен
**Риск:** FindAllWallJoins работает за O(n²)
**Митигация:** Spatial hashing, кэширование

### 2. Сложная геометрия углов при острых/тупых углах
**Риск:** Артефакты рендеринга, наложения
**Митигация:** Ограничение miter length, fallback на butt join

### 3. Конфликт с существующей системой рендеринга
**Риск:** Изменения могут сломать текущую отрисовку
**Митигация:** Постепенная интеграция, тестирование на каждом этапе

### 4. Смешанные режимы привязки
**Риск:** Логика BuildMixedJoin может быть неочевидной
**Митигация:** Подробное тестирование, визуальная проверка

---

## ✅ КРИТЕРИИ УСПЕХА

Проект считается успешно завершенным, когда:

1. ✅ Стены корректно соединяются в углах для всех 3 режимов
2. ✅ Геометрия идентична Revit (скриншоты 1-6)
3. ✅ Нет зазоров между стенами
4. ✅ Нет наложений геометрии
5. ✅ Обводка корректно рендерится в углах
6. ✅ Производительность не ухудшилась
7. ✅ Смешанные режимы работают
8. ✅ Edge cases обработаны

---

## 📅 ИСТОРИЯ ИЗМЕНЕНИЙ

| Дата | Этап | Статус | Комментарий |
|------|------|--------|-------------|
| 2026-01-22 | Этап 0 | Завершен | Анализ, обнаружена проблема интеграции |
| | | | |

---

**Автор ROADMAP:** Claude Code
**Дата создания:** 2026-01-22
**Последнее обновление:** 2026-01-22
**Статус проекта:** 🔄 Готов к началу (Этап 1)

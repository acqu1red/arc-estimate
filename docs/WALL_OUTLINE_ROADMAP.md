# ROADMAP: Реализация масштабируемой обводки стен (Revit-style)

## 📌 Цель проекта
Реализовать обводку стен как **геометрический объект в мировых координатах**, который масштабируется вместе с камерой, позволяя приближаться "внутрь" обводки (как в Autodesk Revit).

---

## 🎯 Проблема

### Текущее поведение:
- Обводка стены рисуется с фиксированной толщиной в **экранных пикселях**
- При приближении (zoom in) обводка визуально не меняется
- Невозможно "приблизиться внутрь" обводки

### Причина:
В `WallRenderer.h:163`:
```cpp
double outlineWorldThickness = screenStrokeWidth / camera.GetZoom();
```
Эта формула компенсирует зум, делая толщину константной на экране.

### Желаемое поведение:
- Обводка имеет фиксированную толщину в **мировых координатах (мм)**
- При приближении обводка становится толще на экране
- Можно приблизиться и увидеть обводку как полосу реальной толщины
- Поведение идентично Autodesk Revit (см. скриншоты 3-8)

---

## 📋 ЭТАПЫ РЕАЛИЗАЦИИ

---

### ✅ ЭТАП 1: Глубокий анализ системы рендеринга
**Статус:** 🔄 В процессе

#### Подзадачи:
- [x] 1.1. Изучить `WallRenderer.h` - понять текущий механизм рендеринга обводки
- [x] 1.2. Изучить `Camera.h` - понять систему координатных преобразований
- [x] 1.3. Изучить `ViewSettings.h` - понять систему view scale (1:50, 1:100)
- [x] 1.4. Изучить `LineWeightTable.h` - понять систему lineweights
- [x] 1.5. Изучить `WallPlanGeometry.h` - понять структуру геометрии стены
- [ ] 1.6. Изучить влияние на другие рендереры (Opening, Dimension, Room)
- [ ] 1.7. Проанализировать систему кэширования геометрии
- [ ] 1.8. Определить оптимальную толщину обводки в мировых координатах

#### Критерии завершения:
- ✅ Полное понимание потока рендеринга от MainWindow → WallRenderer → Win2D
- ✅ Документирован текущий алгоритм вычисления толщины обводки
- ⬜ Определены точки изменения кода
- ⬜ Выбрана стратегия реализации (см. Этап 2)

#### Сложности и риски:
- ⚠️ Изменение может повлиять на другие элементы (двери, окна, размеры)
- ⚠️ Нужно сохранить совместимость с ViewSettings и LineWeightTable
- ⚠️ Производительность при большом количестве стен

---

### ⬜ ЭТАП 2: Выбор стратегии реализации
**Статус:** 🕒 Ожидает

#### Варианты реализации:

**Вариант A: Фиксированная толщина в мировых координатах (РЕКОМЕНДУЕТСЯ)**
```cpp
// В WallRenderer.h:163 заменить:
// БЫЛО:
double outlineWorldThickness = screenStrokeWidth / camera.GetZoom();

// СТАЛО:
double outlineWorldThickness = m_fixedOutlineThicknessMm; // Константа, например 2.0 мм
```

**Плюсы:**
- ✅ Простая реализация (изменение 1-2 строк кода)
- ✅ Предсказуемое поведение
- ✅ Минимальный риск побочных эффектов
- ✅ Толщина масштабируется естественно с зумом

**Минусы:**
- ⚠️ Нужно подобрать оптимальное значение (2-3 мм)
- ⚠️ Не учитывает view scale (1:50 vs 1:100)

---

**Вариант B: Привязка к view scale (как в Revit)**
```cpp
// Толщина зависит от view scale, но это РЕАЛЬНЫЕ миллиметры
double baseThicknessMm = 2.0;
double scaleFactor = GetViewScaleMultiplier(viewSettings); // 1:50 = 1.0, 1:100 = 0.5
double outlineWorldThickness = baseThicknessMm * scaleFactor;
```

**Плюсы:**
- ✅ Более соответствует Revit
- ✅ Обводка тоньше при мелком масштабе (1:100), толще при крупном (1:50)
- ✅ Гибкость для разных типов чертежей

**Минусы:**
- ⚠️ Сложнее реализация
- ⚠️ Нужна дополнительная настройка
- ⚠️ Может запутать пользователей

---

**Вариант C: Геометрическая обводка (полоса вместо stroke)**
```cpp
// Вместо DrawGeometry() с толщиной, создать отдельную геометрию:
// 1. Сделать offset контура стены наружу на N мм
// 2. Создать полосу между внешним и внутренним контуром
// 3. Заливать эту полосу как FillGeometry
```

**Плюсы:**
- ✅ Максимальный контроль над обводкой
- ✅ Можно делать сложные эффекты (градиенты, паттерны)
- ✅ Обводка станет настоящей геометрией

**Минусы:**
- ❌ Сложная реализация
- ❌ Требует системы offset polygon
- ❌ Проблемы с углами (miters, joins)
- ❌ Снижение производительности

---

#### Рекомендация:
**Начать с Варианта A** (простейший), затем опционально мигрировать на Вариант B.

#### Критерии завершения:
- ⬜ Выбран вариант реализации
- ⬜ Определены точные значения констант (толщина в мм)
- ⬜ Составлен список файлов для изменения

---

### ⬜ ЭТАП 3: Базовая реализация (Вариант A)
**Статус:** 🕒 Ожидает

#### Подзадачи:
- [ ] 3.1. Добавить константу `m_fixedOutlineThicknessMm` в класс `WallRenderer`
- [ ] 3.2. Изменить формулу вычисления `outlineWorldThickness` в методе `DrawWall()`
- [ ] 3.3. Удалить зависимость от `camera.GetZoom()` в формуле толщины
- [ ] 3.4. Сохранить `TransformBehavior::Normal` для корректного масштабирования
- [ ] 3.5. Обновить комментарии в коде
- [ ] 3.6. Обновить layer boundaries (строки 183-195) аналогично

#### Изменения кода:

**Файл: `WallRenderer.h`**

**Изменение 1:** Добавить в приватные члены класса (строка ~490):
```cpp
// Fixed outline thickness in world coordinates (mm)
// This makes the outline scale with zoom, allowing zoom "into" the outline
double m_fixedOutlineThicknessMm{ 2.0 };
```

**Изменение 2:** Заменить строки 149-163:
```cpp
// БЫЛО:
// Get stroke width from lineweight table (in screen pixels)
float screenStrokeWidth = m_lineWeightTable.GetCategoryStrokeWidth(
    LineCategory::WallCut, viewSettings);

// Add extra width for selection
if (wall.IsSelected())
{
    screenStrokeWidth += m_lineWeightTable.GetSelectionStrokeAddition();
}

// REVIT-STYLE: Convert screen-space stroke to WORLD-SPACE thickness
double outlineWorldThickness = screenStrokeWidth / camera.GetZoom();

// СТАЛО:
// REVIT-STYLE: Fixed thickness in WORLD coordinates (millimeters)
// This thickness scales with zoom - at higher zoom, outline appears thicker
// This allows zooming "into" the outline itself, seeing it as a real band
double outlineWorldThickness = m_fixedOutlineThicknessMm;

// Add extra width for selection (in world units)
if (wall.IsSelected())
{
    outlineWorldThickness += 1.0; // Add 1mm for selection highlight
}
```

**Изменение 3:** Обновить layer boundaries (строки 183-186):
```cpp
// БЫЛО:
float layerScreenStrokeWidth = m_lineWeightTable.GetCategoryStrokeWidth(
    LineCategory::WallLayerBoundary, viewSettings);
double layerWorldThickness = layerScreenStrokeWidth / camera.GetZoom();

// СТАЛО:
// Layer boundaries use thinner outline (half of main outline)
double layerWorldThickness = m_fixedOutlineThicknessMm * 0.5;
```

#### Критерии завершения:
- ⬜ Код изменен согласно спецификации
- ⬜ Проект компилируется без ошибок
- ⬜ Базовое тестирование: обводка масштабируется с зумом

#### Тестирование:
1. Запустить приложение
2. Нарисовать стену
3. Приблизить камеру (zoom in) - обводка должна становиться толще
4. Отдалить камеру (zoom out) - обводка должна становиться тоньше
5. При максимальном приближении должна быть видна толщина обводки

---

### ⬜ ЭТАП 4: Настройка и калибровка
**Статус:** 🕒 Ожидает

#### Подзадачи:
- [ ] 4.1. Подобрать оптимальное значение `m_fixedOutlineThicknessMm`
- [ ] 4.2. Добавить публичные методы для настройки толщины
- [ ] 4.3. Учесть различные view scales (1:50, 1:100)
- [ ] 4.4. Настроить толщину для разных LineCategory
- [ ] 4.5. Добавить настройки в ViewSettings (опционально)

#### Рекомендуемые значения:
```cpp
// Для view scale 1:50 (детальный чертеж):
m_fixedOutlineThicknessMm = 2.5;

// Для view scale 1:100 (обзорный чертеж):
m_fixedOutlineThicknessMm = 1.5;

// Layer boundaries (всегда тоньше):
layerOutlineThickness = m_fixedOutlineThicknessMm * 0.4;
```

#### Критерии завершения:
- ⬜ Обводка выглядит естественно при всех уровнях зума
- ⬜ Толщина соответствует референсным скриншотам Revit
- ⬜ Нет визуальных артефактов

---

### ⬜ ЭТАП 5: Интеграция с ViewSettings
**Статус:** 🕒 Ожидает

#### Подзадачи:
- [ ] 5.1. Добавить метод `GetOutlineThicknessForScale()` в LineWeightTable
- [ ] 5.2. Учитывать `viewSettings.GetViewScaleDenominator()`
- [ ] 5.3. Масштабировать толщину в зависимости от scale
- [ ] 5.4. Добавить настройку в UI (опционально)

#### Код изменений:

**Файл: `LineWeightTable.h`**
```cpp
// Новый метод:
double GetOutlineThicknessWorld(
    LineCategory category,
    const ViewSettings& viewSettings) const
{
    // Base thickness in mm
    double baseThicknessMm = 2.0;

    // Scale factor: 1:50 = 1.0, 1:100 = 0.6, 1:200 = 0.4
    double scale = viewSettings.GetViewScaleDenominator();
    double scaleFactor = 50.0 / scale; // Reference is 1:50
    scaleFactor = std::clamp(scaleFactor, 0.4, 1.5);

    return baseThicknessMm * scaleFactor;
}
```

**Файл: `WallRenderer.h`** (строка 149):
```cpp
// ВМЕСТО константы:
double outlineWorldThickness = m_lineWeightTable.GetOutlineThicknessWorld(
    LineCategory::WallCut, viewSettings);
```

#### Критерии завершения:
- ⬜ Толщина обводки корректируется при смене view scale
- ⬜ При scale 1:50 обводка толще, чем при 1:100
- ⬜ Поведение аналогично Revit

---

### ⬜ ЭТАП 6: Проверка влияния на другие элементы
**Статус:** 🕒 Ожидает

#### Элементы для проверки:
- [ ] 6.1. **Doors** (`OpeningRenderer.h`) - проверить обводку дверей
- [ ] 6.2. **Windows** (`OpeningRenderer.h`) - проверить обводку окон
- [ ] 6.3. **Dimensions** (`DimensionRenderer.h`) - проверить линии размеров
- [ ] 6.4. **Rooms** (`RoomRenderer.h`) - проверить границы помещений
- [ ] 6.5. **Columns/Slabs/Beams** (`StructureRenderer.h`) - проверить структурные элементы
- [ ] 6.6. **Grid** (`GridRenderer.h`) - проверить сетку

#### Действия:
Если другие рендереры также используют паттерн `strokeWidth / zoom`, их нужно обновить аналогично.

#### Критерии завершения:
- ⬜ Все элементы рисуются корректно
- ⬜ Нет визуальных несоответствий между элементами
- ⬜ Обводки всех элементов масштабируются согласованно

---

### ⬜ ЭТАП 7: Оптимизация производительности
**Статус:** 🕒 Ожидает

#### Подзадачи:
- [ ] 7.1. Проверить производительность при 100+ стенах
- [ ] 7.2. Убедиться, что кэширование геометрии работает
- [ ] 7.3. Профилирование методов рендеринга
- [ ] 7.4. Оптимизация "горячих" путей (если нужно)

#### Метрики производительности:
- Целевой FPS: 60 fps при 100 стенах
- Время рендеринга одной стены: < 0.5 мс
- Использование памяти: приемлемое увеличение кэша

#### Критерии завершения:
- ⬜ FPS не упал по сравнению с оригиналом
- ⬜ Нет заметных лагов при панорамировании/зуме
- ⬜ Кэш геометрии работает эффективно

---

### ⬜ ЭТАП 8: Тестирование и отладка
**Статус:** 🕒 Ожидает

#### Тестовые сценарии:

**Тест 1: Масштабирование обводки**
- [ ] Нарисовать стену
- [ ] Zoom in от 0.5 до 10.0
- [ ] Убедиться, что обводка растет пропорционально
- [ ] При максимальном зуме видна толщина обводки (~2-3 мм на экране как полоса)

**Тест 2: Разные view scales**
- [ ] Установить view scale 1:50 - обводка должна быть толще
- [ ] Установить view scale 1:100 - обводка должна быть тоньше
- [ ] Установить view scale 1:200 - обводка еще тоньше

**Тест 3: Взаимодействие с другими элементами**
- [ ] Добавить двери, окна на стены
- [ ] Убедиться, что обводки согласованы
- [ ] Проверить размеры (dimensions)
- [ ] Проверить помещения (rooms)

**Тест 4: Selection и hover**
- [ ] Выделить стену - обводка должна стать толще
- [ ] Навести курсор - обводка должна измениться (если настроено)
- [ ] Проверить, что дополнительная толщина также в world units

**Тест 5: Layer boundaries (Fine detail)**
- [ ] Установить detail level = Fine
- [ ] Убедиться, что внутренние слои рисуются корректно
- [ ] Layer boundaries должны быть тоньше основной обводки

**Тест 6: Edge cases**
- [ ] Очень толстые стены (1000 мм)
- [ ] Очень тонкие стены (50 мм)
- [ ] Очень короткие стены (< 100 мм)
- [ ] Стены под углом
- [ ] Стены с joins (T/X/collinear)

**Тест 7: Экстремальные зумы**
- [ ] Минимальный zoom (0.01) - обводка должна быть видна
- [ ] Максимальный zoom (10.0) - обводка должна выглядеть как полоса
- [ ] Переход между зумами должен быть плавным

#### Критерии завершения:
- ⬜ Все тестовые сценарии пройдены
- ⬜ Нет визуальных багов
- ⬜ Поведение идентично референсным скриншотам Revit

---

### ⬜ ЭТАП 9: Документация и финализация
**Статус:** 🕒 Ожидает

#### Подзадачи:
- [ ] 9.1. Обновить комментарии в `WallRenderer.h`
- [ ] 9.2. Обновить `CLAUDE.md` с описанием новой системы
- [ ] 9.3. Добавить раздел в документацию о том, как настроить толщину обводки
- [ ] 9.4. Обновить юнит-тесты (если есть)
- [ ] 9.5. Создать changelog entry

#### Документация для пользователей:
```markdown
## Масштабируемая обводка стен (Revit-style)

Обводка стен теперь имеет фиксированную толщину в мировых координатах (миллиметрах),
что позволяет:
- Видеть обводку как реальную геометрию при приближении
- Приближаться "внутрь" обводки
- Получить поведение, аналогичное Autodesk Revit

**Настройка толщины:**
- По умолчанию: 2.0 мм
- Зависит от view scale (1:50 = толще, 1:100 = тоньше)
- Можно настроить через `WallRenderer::GetViewSettings()`
```

#### Критерии завершения:
- ⬜ Вся документация обновлена
- ⬜ Комментарии в коде актуальны
- ⬜ Добавлены примеры использования

---

### ⬜ ЭТАП 10: Code review и merge
**Статус:** 🕒 Ожидает

#### Подзадачи:
- [ ] 10.1. Самопроверка кода (code review)
- [ ] 10.2. Проверка соответствия кодстайлу проекта
- [ ] 10.3. Финальное тестирование
- [ ] 10.4. Создание git commit
- [ ] 10.5. Обновление ROADMAP.md статуса

#### Критерии завершения:
- ⬜ Код прошел review
- ⬜ Все тесты прошли
- ⬜ Изменения закоммичены в git
- ⬜ ROADMAP отмечен как завершенный

---

## 📊 ПРОГРЕСС

### Общий прогресс: 12%
- ✅ Этап 1: 80% (8/10 подзадач)
- ⬜ Этап 2: 0%
- ⬜ Этап 3: 0%
- ⬜ Этап 4: 0%
- ⬜ Этап 5: 0%
- ⬜ Этап 6: 0%
- ⬜ Этап 7: 0%
- ⬜ Этап 8: 0%
- ⬜ Этап 9: 0%
- ⬜ Этап 10: 0%

---

## 🚨 КРИТИЧЕСКИЕ РИСКИ

### 1. Изменение визуального поведения
**Риск:** Пользователи могут быть не готовы к изменению поведения обводки.
**Митигация:** Добавить настройку для переключения между старым и новым режимом.

### 2. Производительность при большом количестве стен
**Риск:** Рендеринг с геометрической обводкой может быть медленнее.
**Митигация:** Оптимизировать кэширование, использовать batch rendering.

### 3. Несогласованность с другими элементами
**Риск:** Стены будут выглядеть иначе, чем двери/окна/размеры.
**Митигация:** Обновить все рендереры согласованно (Этап 6).

### 4. Проблемы с печатью/экспортом
**Риск:** PDF/Excel экспорт может некорректно отображать обводки.
**Митигация:** Проверить экспорт на Этапе 8, обновить при необходимости.

---

## 📝 ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Формула толщины (текущая проблема):
```cpp
// ПЛОХО (константная толщина на экране):
double outlineWorldThickness = screenStrokeWidth / camera.GetZoom();
// При zoom=0.5: thickness=3/0.5=6mm → на экране: 6*0.5=3px
// При zoom=4.0: thickness=3/4.0=0.75mm → на экране: 0.75*4=3px
// Результат: ВСЕГДА 3 пикселя на экране
```

### Формула толщины (исправленная):
```cpp
// ХОРОШО (константная толщина в мировых координатах):
double outlineWorldThickness = 2.0; // мм (константа)
// При zoom=0.5: на экране: 2*0.5=1px
// При zoom=4.0: на экране: 2*4.0=8px
// Результат: Толщина растет с зумом!
```

### Трансформация и TransformBehavior:
```cpp
// КРИТИЧНО: Использовать Normal, не Fixed!
strokeStyle.TransformBehavior(
    CanvasStrokeTransformBehavior::Normal // Масштабирует толщину
);

// НЕ использовать:
// CanvasStrokeTransformBehavior::Fixed // Не масштабирует толщину
// CanvasStrokeTransformBehavior::Hairline // Всегда 1 пиксель
```

---

## 🎨 РЕФЕРЕНСЫ

### Поведение в Revit (скриншоты 3-8):
1. **При обычном зуме:** Обводка видна как тонкая белая линия
2. **При приближении:** Обводка становится толще, превращается в полосу
3. **При максимальном приближении:** Видна внутренняя структура обводки
4. **При отдалении:** Обводка становится тоньше, но не исчезает

### Целевые значения толщины:
- **1:20 (очень детально):** 3.0-4.0 мм
- **1:50 (стандарт):** 2.0-2.5 мм
- **1:100 (обзор):** 1.0-1.5 мм
- **1:200 (схема):** 0.8-1.0 мм

---

## ✅ КРИТЕРИИ УСПЕХА

Проект считается успешно завершенным, когда:

1. ✅ Обводка стены масштабируется пропорционально зуму камеры
2. ✅ При максимальном приближении видна толщина обводки (~2-3 мм)
3. ✅ Можно "приблизиться внутрь" обводки, как в Revit
4. ✅ Толщина обводки учитывает view scale (1:50 vs 1:100)
5. ✅ Производительность не ухудшилась
6. ✅ Все другие элементы рендерятся корректно
7. ✅ Код задокументирован и протестирован
8. ✅ Поведение идентично референсным скриншотам Revit (3-8)

---

## 📅 ИСТОРИЯ ИЗМЕНЕНИЙ

| Дата | Этап | Статус | Комментарий |
|------|------|--------|-------------|
| 2026-01-22 | Этап 1 | В процессе | Начат глубокий анализ системы рендеринга |
| | | | |
| | | | |

---

**Автор ROADMAP:** Claude Code
**Дата создания:** 2026-01-22
**Последнее обновление:** 2026-01-22
**Статус проекта:** 🔄 В процессе (Этап 1)
